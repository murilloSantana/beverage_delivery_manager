version: '3.7'
services:
  mongo:
    image: mongo:4.0.12
    ports:
      - 27017:27017
    environment:
      MONGO_INITDB_DATABASE: beverageDeliveryManagerDB
    volumes:
      - ./mongo:/imports
      - ./mongo:/docker-entrypoint-initdb.d/
